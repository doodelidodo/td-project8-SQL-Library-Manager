extends ../layout

block content
    h2= title

    include errors

    form(action="/books/" + book.id, method="post")
        include form
        p
            input(type='submit' value='Update Book')
        p
            a.button(href='/') Cancel

    include delete